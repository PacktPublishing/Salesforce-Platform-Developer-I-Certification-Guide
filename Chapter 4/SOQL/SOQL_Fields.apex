
SELECT COUNT() FROM ACCOUNT; // this query will return an integer, the number of all accounts in your database
SELECT toLabel(picklistfield) FROM ACCOUNT; // This query will provide you the translated version of a picklist value. If you translate your picklist values with the translation workbench, with the toLabel(field), you receive the translated value in the user’s language.
